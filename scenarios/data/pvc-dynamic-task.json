{
  "id": "pvc-dynamic-task",
  "title": "Dynamiczne tworzenie PVC",
  "category": "Storage",
  "difficulty": "Medium",
  "type": "task",
  "weight": 6,
  "description": "## Dynamiczne tworzenie PVC\n\nDynamiczne tworzenie automatycznie tworzy PersistentVolume gdy powstaje PVC, używając StorageClass. k3s zawiera wbudowany StorageClass `local-path`.\n\n**Twoje zadanie:**\n\n1. Utwórz **PersistentVolumeClaim** o nazwie `app-data` żądający:\n   - Pojemność: `1Gi`\n   - Tryb dostępu: `ReadWriteOnce`\n   - StorageClass: `local-path`\n2. Utwórz Pod o nazwie `data-pod` używający `nginx:alpine`, który montuje `app-data` w `/data`\n\n```bash\nkubectl get storageclass   # sprawdź czy local-path istnieje\n```",
  "hints": [
    {
      "title": "Manifest PVC",
      "body": "Ustaw `storageClassName: local-path` (domyślne k3s). Dynamiczne tworzenie tworzy PV automatycznie.",
      "command": "cat <<EOF | kubectl apply -f -\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: app-data\nspec:\n  accessModes: [ReadWriteOnce]\n  storageClassName: local-path\n  resources:\n    requests:\n      storage: 1Gi\nEOF"
    },
    {
      "title": "Zamontuj PVC w Podzie",
      "body": "Odwołaj się do PVC po nazwie w `spec.volumes[].persistentVolumeClaim.claimName`.",
      "command": "cat <<EOF | kubectl apply -f -\napiVersion: v1\nkind: Pod\nmetadata:\n  name: data-pod\nspec:\n  containers:\n  - name: app\n    image: nginx:alpine\n    volumeMounts:\n    - name: storage\n      mountPath: /data\n  volumes:\n  - name: storage\n    persistentVolumeClaim:\n      claimName: app-data\nEOF"
    }
  ],
  "setup_commands": [],
  "validation": {
    "commands": [
      {
        "description": "PVC 'app-data' istnieje",
        "command": "kubectl get pvc app-data -o jsonpath='{.metadata.name}'",
        "expected_output": "app-data",
        "match": "exact"
      },
      {
        "description": "PVC żąda pojemności 1Gi",
        "command": "kubectl get pvc app-data -o jsonpath='{.spec.resources.requests.storage}'",
        "expected_output": "1Gi",
        "match": "exact"
      },
      {
        "description": "Pod 'data-pod' montuje PVC",
        "command": "kubectl get pod data-pod -o jsonpath='{.spec.volumes[0].persistentVolumeClaim.claimName}'",
        "expected_output": "app-data",
        "match": "exact"
      },
      {
        "description": "Pod 'data-pod' jest Running",
        "command": "kubectl get pod data-pod -o jsonpath='{.status.phase}'",
        "expected_output": "Running",
        "match": "exact"
      }
    ]
  },
  "default_namespace": "default",
  "teardown_commands": [
    {
      "command": "kubectl delete pvc app-data --ignore-not-found"
    },
    {
      "command": "kubectl delete pod data-pod --ignore-not-found --grace-period=0 --force"
    }
  ]
}