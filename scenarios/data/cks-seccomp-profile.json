{
  "id": "cks-seccomp-profile",
  "title": "Profile Seccomp",
  "category": "Workload Security",
  "difficulty": "Hard",
  "type": "task",
  "weight": 6,
  "description": "## Tryb bezpiecznego przetwarzania (Seccomp)\n\nSeccomp ogranicza wywołania systemowe, które kontener może kierować do jądra hosta.\n\n**Twoje zadanie:**\n\nUtwórz Pod o nazwie `seccomp-pod` używający obrazu `busybox:1.36` (komenda: `sleep 3600`). Skonfiguruj poda tak, aby typ profilu seccomp był ustawiony na `RuntimeDefault`.\n\n```bash\n# Wskazówka: przejrzyj dokumentację PodSecurityContext\n```",
  "hints": [
    {
      "title": "Ustaw seccompProfile",
      "body": "Dodaj `seccompProfile: { type: RuntimeDefault }` do securityContext poda.",
      "command": "cat <<EOF | kubectl apply -f -\napiVersion: v1\nkind: Pod\nmetadata:\n  name: seccomp-pod\nspec:\n  securityContext:\n    seccompProfile:\n      type: RuntimeDefault\n  containers:\n  - name: main\n    image: busybox:1.36\n    command: [\"sleep\", \"3600\"]\nEOF"
    }
  ],
  "setup_commands": [],
  "validation": {
    "commands": [
      {
        "description": "Typ seccompProfile to RuntimeDefault",
        "command": "kubectl get pod seccomp-pod -o jsonpath='{.spec.securityContext.seccompProfile.type}'",
        "expected_output": "RuntimeDefault",
        "match": "exact"
      }
    ]
  },
  "default_namespace": "default",
  "teardown_commands": [
    {
      "command": "kubectl delete pod seccomp-pod --ignore-not-found --grace-period=0 --force"
    }
  ]
}